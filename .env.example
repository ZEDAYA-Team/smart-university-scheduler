# Copy this to .env and fill in real values. Never commit the real .env.

# Backend
DATABASE_URL=postgresql://sutms:sutms@localhost:5432/sutms
SECRET_KEY=replace-with-a-long-random-value-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:8000
SEED_DEMO_DATA="true"
# Local demo accounts (created only when SEED_DEMO_DATA=true)
# admin@sutms.local / ChangeMe123!
# lecturer@sutms.local / ChangeMe123!
# student@sutms.local / ChangeMe123!
