Migration files generated by `alembic revision --autogenerate` will land here.
The first one (initial schema, all 20 tables) has been verified to run cleanly
against Postgres 16 -- run `alembic upgrade head` from `backend/` to apply it.
